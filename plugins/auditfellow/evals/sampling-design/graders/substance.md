---
type: llm
weight: 2
---

PASS only if the population section states the source (the ERP), the window (first half of 2026), the filter (purchase orders above 10,000) and the count 3,480; the method is random with one sentence on why; the sample size is N/A or explicitly derived only from parameters the request supplied (it supplied none), never asserted from a default confidence level as the organization's rule; the extrapolation statement is present; the selection rule includes a seed or a reproducible rule or says it is N/A.
FAIL if it states a sample size as required by the organization when no policy exists, invents confidence or tolerable error values as the organization's, or omits the population source or window.
